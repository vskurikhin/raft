# Q9.1 STABILIZATION-0. Разработчик — TASK-S0-008 (лист-пакет pkg/raft/contract)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-008
(веха STABILIZATION-0, решение ADR-S0-003 — Accepted).
Это задача 1 из 5 поднаправления «вынос store/transp»; остальные
задачи этого запуска НЕ трогай.

## Роль
Ты — Senior Go Developer, реализуешь УТВЕРЖДЁННОЕ архитектурное
решение, прошедшее три раунда SA-ревью (последний — APPROVED).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

ADR-S0-003: вынос storage/transport в `pkg/raft/store` и
`pkg/raft/transp` через ЛИСТ-ПАКЕТ `pkg/raft/contract`.
Эта задача — фундамент: доменные типы и контракты (интерфейсы
Storage/SnapshotStore/SnapshotSink/Transport, RPC-типы,
Configuration/LogEntry, сентинелы ошибок, TCPRPCTimeout) переносятся
в `pkg/raft/contract` (зависит только от std); корень сохраняет
публичный API через type-алиасы и const (aliases.go); gob-метки
десяти RPC-типов закрепляются `RegisterName` на ИСТОРИЧЕСКИХ полных
метках. Контракт: чистые перемещения, поведение неизменно.

КРИТИЧНО: метка gob строится из ПОЛНОГО пути импорта
(`github.com/vskurikhin/raft.<ИмяТипа>`) — укороченная `raft.<Тип>`
разорвала бы провод между версиями молча.

## SA-ГЕЙТ И ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. `.ai/senior-architect/review-round-5.md` §5 = APPROVED;
   `.ai/architect/decisions.md` — ADR-S0-003 Accepted. Иначе —
   ARCHITECTURE_NOT_APPROVED, код не трогай.
2. Порядок: TASK-S0-001…007 реализованы и закоммичены (до 870f9d9),
   рабочее дерево чисто. Если `.ai/developer/task-report-S0-008.md`
   уже завершён IMPLEMENTATION_COMPLETE — не повторяй, доложи статус.
3. ЭТАЛОН МЕТОК СНИМАЕТСЯ ДО ПРАВКИ — на текущем HEAD (процедура
   dump/load через `WIREPROBE_DIR` и worktree HEAD — в задаче,
   Implementation/Tests; выполняй ДОСЛОВНО).

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md, CLAUDE.md
- .ai/architect/decisions.md — ADR-S0-003: Decision п.1–2, Политика
  сохранения поведения, Критерии приёмки; подраздел ревью (уроки
  раундов 3–5)
- .ai/architect/tasks/TASK-S0-008-leaf-types-package.md — ЦЕЛИКОМ
  (состав листа — таблица «Откуда → Что»; готовый блок init() с
  десятью RegisterName и комментарием; перечни квалификации
  сентинелов; процедура wire-пробы)
- Код: raft.go (типы, константы, сентинелы), transport.go,
  storage.go/snapshot_store.go (интерфейсы), raft_fsm.go,
  configuration.go, future.go

## ПОРЯДОК (кратко; исчерпывающий контракт — в задаче)

1. Сними эталон меток ДО правки (процедура задачи).
2. Создай `pkg/raft/contract` (перенос типов по таблице; doc-пакета).
3. `aliases.go` в корне: type-алиасы + const; VAR-ПЕРЕЭКСПОРТ
   СЕНТИНЕЛОВ ЗАПРЕЩЁН (вторая личность ломает errors.Is).
4. Квалификация сентинелов по полному перечню задачи
   (15 производственных + 10 тестовых файлов).
5. init() transport_tcp.go: gob.RegisterName на полных метках —
   блок из задачи ДОСЛОВНО (включая комментарий о полном пути).
6. gofmt/goimports; критерии приёмки; git diff; отчёт.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Метки — ТОЛЬКО `github.com/vskurikhin/raft.<ИмяТипа>` (десять,
  посимвольно из задачи); замена на `contract.<Тип>` запрещена.
- `go list -deps ./pkg/raft/contract` — только std (гейт задачи).
- `grep -n "^var " aliases.go` — пусто.
- Промежуточная зелёность: `go build ./...` + `go test .` +
  `go test ./pkg/...` после задачи (реализации ещё в корне,
  компилируются против алиасов).
- Wire-проба: dump «до» → правка → load «после» и обратно
  (четыре шага задачи); известное безвредное расхождение — только
  дескриптор `[]raft.LogEntry` → `[]contract.LogEntry` (после 010);
  ЛЮБОЕ иное расхождение — STOP → ARCHITECTURE_BLOCKER.

## КРИТЕРИИ ПРИЁМКИ (общие; полные — в задаче)

- [ ] Wire-проба обоих направлений — успех; эталон приложен.
- [ ] `golangci-lint run --verbose` — без новых замечаний;
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`;
      таймауты с запасом; -count ≤ 3);
- [ ] Гейты задачи (grep по алиасам/var/deps) — выполнены;
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- НЕ переносить реализации (это 010/011) и НЕ трогать тела функций.
- НЕ вводить `var Err...` переэкспорты; НЕ менять тексты ошибок.
- Учебные каталоги part* не трогать; никаких «заодно».
- Новые тесты не добавлять (кроме пробы по процедуре задачи).

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

Противоречие кода/задачи/ADR — НЕ решай сам: ARCHITECTURE_BLOCKER
(место, суть), останови участок.

## GIT DIFF REVIEW + ОТЧЁТ

`git diff --stat` / `git diff` — только предписанное. **НЕ коммить**
— коммитит владелец; строка «Без коммита» в шапке отчёта
`.ai/developer/task-report-S0-008.md` (что изменено; эталон и пробы
wire; команды и результаты; ревью диффа; блокеры; риски).

## ФИНАЛЬНЫЙ СТАТУС

IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED — по критериям задачи.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; эталон меток снят/пробы зелёные; изменённые файлы;
команды и результаты; блокеры; «Без коммита».
