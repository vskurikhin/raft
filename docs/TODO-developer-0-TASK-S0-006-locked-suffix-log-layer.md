# Q5.1 STABILIZATION-0. Разработчик — TASK-S0-006 (суффикс Locked, журнальный слой)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-006
(веха STABILIZATION-0, решение ADR-S0-002 — Proposed).
Это задача 1 из 2 поднаправления «Locked-имена»; задача 007 этого
запуска НЕ трогай.

## РАЗРЕШЕНИЕ НА ДОСРОЧНЫЙ СТАРТ (решение владельца 2026-09-02)

SA-ревью ADR-S0-002 ещё НЕ проведено. Владелец явным решением
разрешил реализацию ДО вердикта: замечания, если появятся,
устраняются после — правкой архитектуры и кода. Следствия для тебя:

- это НЕ отменяет ARCHITECTURE_BLOCKER-дисциплину: противоречия
  между кодом, задачей и ADR не решаешь сам — фиксируешь блокер;
- дифф может быть переработан по итогам ревью: все артефакты
  (grep-выводы) сохраняй в отчёте, чтобы не переснимать;
- ADR-S0-002 остаётся в статусе Proposed — статусы задач от этого
  не зависят.

## Роль
Ты — Senior Go Developer, реализуешь архитектурное решение
(переименования по контракту дисциплины блокировок).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

Контракт AGENTS.md: функция, требующая удержания cm.mu, обязна
иметь суффикс `Locked` и канонический комментарий
«Требует удержания cm.mu.»; `Unsafe` устарел.

Эта задача — журнальный слой `raft_cm_log.go` и все места вызова:
ШЕСТЬ переименований (каждое — только идентификатор и doc,
никаких изменений тел, сигнатур-типов и порядка вызовов):

    lastLogIndexAndTerm   → lastLogIndexAndTermLocked
    rebuildLastLog        → rebuildLastLogLocked
    rebuildTermIndexMap   → rebuildTermIndexMapLocked
    setLastLog            → setLastLogLocked
    compactLogs           → compactLogsLocked
    dispatchLogsUnsafe    → dispatchLogsLocked   (устаревший суффикс)

Плюс: канонизация doc-комментариев и оговорка стартового пути
в doc `restoreFromStorage`. Полные таблицы «Было → Стало» по каждому
вхождению (production, тесты, текстовые упоминания — включая
сообщение `t.Fatalf` в `raft_snapshot_test.go` и комментарии
в тестах) — в задаче; таблица является исчерпывающим контрактом.

## ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. Последовательность вехи: `.ai/developer/task-report-S0-005.md`
   существует и завершён IMPLEMENTATION_COMPLETE. Иначе — STOP
   с сообщением TASK_ORDER_VIOLATION (зависимость от TASK-S0-005
   строгая: сдвиг строк и пересечение зон в raft_cm_rpc.go /
   raft_cm_replication.go).
2. `.ai/architect/decisions.md` содержит раздел ADR-S0-002
   (статус Proposed — допустим при досрочном старте, см. выше).
   Если раздела нет — ARCHITECTURE_NOT_APPROVED.
3. Идемпотентность: если `task-report-S0-006.md` уже завершён
   COMPLETE — не повторяй, доложи статус.
4. В рабочем дереве может лежать незакоммиченный дифф задач S0
   (коммитит владелец): НЕ откатывай и не правь чужое; твой дифф —
   поверх.
5. Номера строк в задаче ориентировочные (дерево сдвигалось
   задачами S0): места правки ищи по ИМЕНИ функции и тексту
   «Было» из таблиц, не по номерам.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md — раздел «Дисциплина блокировок» (предмет задачи) +
  «Тестовые приспособления» + терминология
- CLAUDE.md
- .ai/architect/decisions.md — ADR-S0-002: Decision п.1, 3, 4;
  Alternatives; Risks
- .ai/architect/tasks/TASK-S0-006-locked-suffix-log-layer.md —
  ЦЕЛИКОМ (таблицы замен, дословные doc-тексты, Non-Goals,
  Risks, Acceptance Criteria)
- Код: `raft_cm_log.go` целиком; `raft_cm_storage.go`
  (restoreFromStorage); места вызовов по таблице; тестовые файлы
  из таблицы

## ПОРЯДОК

1. Сними базовые артефакты «до» (в отчёт):

```bash
    grep -rn -w 'lastLogIndexAndTerm' --include='*.go' . | grep -v '/part'
    grep -rn -w 'rebuildLastLog' --include='*.go' . | grep -v '/part'
    grep -rn -w 'rebuildTermIndexMap' --include='*.go' . | grep -v '/part'
    grep -rn -w 'setLastLog' --include='*.go' . | grep -v '/part'
    grep -rn -w 'compactLogs' --include='*.go' . | grep -v '/part'
    grep -rn -w 'dispatchLogsUnsafe' --include='*.go' . | grep -v '/part'
```

   Сверь перечень с таблицей задачи: РАСХОЖДЕНИЕ (вхождение вне
   таблицы) = STOP → ARCHITECTURE_BLOCKER (не расширяй scope сам).

2. Переименуй по таблицам: определения, все вызовы, текстовые
   вхождения (t.Fatalf, комментарии в тестах — по таблице).
3. Приведи doc-комментарии шести функций к каноническим текстам
   ДОСЛОВНО из задачи.
4. Внеси оговорку стартового пути в doc `restoreFromStorage`
   (дословный текст — в задаче).
5. Тестовые вызовы MIXED-пары без Lock (`raft_cm_log_test.go`) —
   строго по предписанию задачи (комментарий приспособления по
   правилам AGENTS.md); ничего сверх.
6. gofmt/goimports по затронутым файлам.
7. Выполни критерии приёмки (ниже).
8. `git diff` — ревью собственных изменений.
9. Заполни `.ai/developer/task-report-S0-006.md`.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Замена меняет ТОЛЬКО идентификаторы и doc-комментарии: любой
  другой символ в диффе — нарушение (операторы, аргументы,
  порядок вызовов, условия — неприкосновенны).
- `rebuildLastLog`/`rebuildTermIndexMap` — ядро восстановления
  после снимка: порядок вызовов не меняется ни в одной точке.
- Ни одного изменения тел функций; учебные каталоги part* не
  трогать (старые имена там остаются — это Non-Goal).
- Каноническая фраза «Требует удержания cm.mu.» — без вариантов
  «(Lock)» и пр.

## КРИТЕРИИ ПРИЁМКИ

- [ ] Для КАЖДОГО из шести старых имён:

      grep -rn -w '<старое имя>' --include='*.go' . | grep -v '/part'

      — пусто (вывод приложить; `-w` не ловит `*Locked`-формы —
      это ожидаемо).
- [ ] Канонические doc у всех шести: `grep -n 'Требует удержания
      cm.mu' raft_cm_log.go` покрывает каждое (сверка с задачей).
- [ ] Оговорка стартового пути в doc `restoreFromStorage` внесена
      дословно.
- [ ] gofmt/goimports по затронутым файлам — без замечаний.
- [ ] `golangci-lint run --verbose` — без новых замечаний.
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`;
      до ~15 минут, таймауты с запасом; -count ≤ 3).
- [ ] `go test -run TestLockOwnershipDiscipline .` — зелёный
      (честная формулировка: тест проверяет симметрию Lock/Unlock
      в функции, не суффиксы).
- [ ] Целевые прогоны тестов, трогающих переименованные функции
      (перечень — в разделе Tests задачи), с `-race`.
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- НЕ переименовывать ничего сверх шести имён (другие функции
  raft_cm_log.go — даже с похожими комментариями — вне задачи;
  см. TASK-S0-007 для пакетных).
- НЕ изменять тела, сигнатуры (кроме имени), порядок вызовов,
  условия, трассировки.
- НЕ «заодно» править прочие неканонические комментарии вне
  перечня задачи.
- Новые тесты не добавлять; существующие не ослаблять
  (переименование вызова ослаблением не является; изменение
  утверждений — да).

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

При противоречии между кодом, задачей, ADR — НЕ решай сам:
создай ARCHITECTURE_BLOCKER (место, суть), останови участок.
Особенно: вхождение имени вне таблицы; тест, который после
переименования не компилируется по неожиданной причине.

## GIT DIFF REVIEW

```bash
    git diff --stat
    git diff
```

Дифф содержит только переименования и doc — ничего больше.
**НЕ коммить** — коммитит владелец; строка «Без коммита»
обязательна в шапке отчёта.

## ФИНАЛЬНЫЙ ОТЧЁТ

`.ai/developer/task-report-S0-006.md` (шапка: задача, статус,
«Без коммита», пометка «досрочный старт до SA-ревью по решению
владельца 02.09»): базовые grep «до» и контрольные «после» по шести
именам; подтверждение соответствия перечня таблице; команды
и результаты; ревью собственного git diff; блокеры; остаточные
риски.

## ФИНАЛЬНЫЙ СТАТУС

Один из: IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED. COMPLETE — только при всех зелёных
критериях.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; шесть grep-критериев пусты; изменённые файлы;
команды и результаты; блокеры; подтверждение «Без коммита».
