# Q3.3 STABILIZATION-0. Разработчик — TASK-S0-003 (предикат дыры снимка)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-003
(веха STABILIZATION-0, решение ADR-S0-001 — Accepted).
Это задача 3 из 5; выполняется строго ПОСЛЕ TASK-S0-002; остальные
задачи этого запуска НЕ трогай.

## Роль
Ты — Senior Go Developer, реализуешь УТВЕРЖДЁННОЕ архитектурное
решение, прошедшее независимое ревью Senior Architect (два раунда).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- НЕ расширяй scope задачи без явного основания.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

ADR-S0-001 — рефакторинг громоздких условных выражений с жёстким
контрактом: правка ДОКАЗУЕМО ТОЖДЕСТВЕННА по поведению.

Эта задача — `raft_cm_replication.go`, `planReplication` (:106–124),
находка BC-004: 4 операнда смешивают nil-проверку интерфейса
хранилища снимков и границы диапазона дыры «снимок/журнал».
Геометрия диапазона уходит в метод-предикат
`prevInSnapshotHoleLocked`; nil-проверка интерфейса ОСТАЁТСЯ на
месте вызова первым операндом (типизированный nil-интерфейс —
реальный случай, `isNilInterface` обязательна).

## ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. SA-вердикт: `.ai/senior-architect/review.md`, раздел «Раунд 2»,
   §6 = APPROVED; `.ai/architect/decisions.md` — ADR-S0-001 Accepted.
   Иначе: ARCHITECTURE_NOT_APPROVED, код не трогай.
2. Последовательность: `.ai/developer/task-report-S0-002.md`
   завершён IMPLEMENTATION_COMPLETE. Иначе — STOP с сообщением
   TASK_ORDER_VIOLATION (порядок 001→…→005 обязателен).
3. Идемпотентность: если `task-report-S0-003.md` уже завершён
   COMPLETE — не повторяй, доложи статус.
4. В рабочем дереве незакоммиченный дифф задач 001–002
   (transport_tcp.go, raft_cm_rpc.go): НЕ откатывай и не правь его;
   твой дифф — поверх.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md (дисциплина блокировок: суффикс Locked + комментарий
  «Требует удержания cm.mu»; терминология)
- CLAUDE.md
- .ai/architect/decisions.md — разделы: Status, «Изменения по ревью
  SA», «Паттерн рефакторинга» (п.1 — методы-предикаты), «Политика
  сохранения поведения», «Критерии приёмки»
- .ai/architect/tasks/TASK-S0-003-predicate-snapshot-hole.md —
  ЦЕЛИКОМ (особенно Target State с точным текстом предиката и doc)
- Код: `raft_cm_replication.go` — `planReplication`, комментарий
  :95–99, условие :118–121; прецеденты предикатов
  (`replicationBackoffActiveLocked` :412)

## ЗАДАЧА (точный текст предиката и doc — в Target State задачи)

1. Новый метод на `*ConsensusModule`:

```go
// <doc из задачи: prev неотрицателен, не совпадает с границей снимка
// (lastSnapshotIndex — при совпадении терм известен из метаданных
// снимка) и меньше первого индекса, для которого лидер может сообщить
// терм. Требует удержания cm.mu.>
func (cm *ConsensusModule) prevInSnapshotHoleLocked(prev int) bool {
    <тело из задачи: три операнда диапазона>
}
```

2. Условие в `planReplication` становится:
   `!isNilInterface(cm.snapshotStore) && cm.prevInSnapshotHoleLocked(prev)`
   (nil-проверка — ПЕРВЫЙ операнд, на месте вызова, порядок сохранён).
3. Комментарий :95–99 (семантика дыры) сохраняется или переносится
   в doc предиката — по тексту задачи.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Предикат читает `cm.cmState` под УЖЕ взятым cm.mu и сам блокировку
  НЕ берёт (`planReplication` берёт cm.mu сама; самозахват = дедлок).
- Тело — только чтения полей, без аллокаций, кандидат на inlining.
- «Снимочная триада» (:57, :119–120, :201) НЕ консолидируется —
  Non-Goal задачи; :57 и :201 не трогать.
- Комментарии по терминологии AGENTS.md, без упоминания ADR/TASK.

## КРИТЕРИИ ПРИЁМКИ

- [ ] `!isNilInterface(cm.snapshotStore)` — первый операнд на месте
      вызова (критерий №1 задачи).
- [ ] Doc предиката — через фактические операнды (текст задачи),
      с «Требует удержания cm.mu».
- [ ] `go build -gcflags=-m . 2>&1 | grep prevInSnapshotHoleLocked`
      — «can inline» (вывод приложить к отчёту).
- [ ] `go test -run TestLockOwnershipDiscipline .` — зелёный.
- [ ] Целевой прогон: `go test -run
      'TestLeaderSendAEsToPeer_SnapshotPredicateBoundaries' -race .`
      — зелёный, отдельно от полного (границы «снимок vs AE» —
      ровно предмет предиката).
- [ ] gofmt/goimports по `raft_cm_replication.go` — без замечаний.
- [ ] `golangci-lint run --verbose` — без новых замечаний.
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`;
      до ~15 минут, таймауты с запасом; -count ≤ 3).
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- Измеримого бенчмарк-гейта на изменяемый код НЕТ и не заявлено —
  НЕ добавлять бенчмарки, НЕ ссылаться на 54/87/54 как на гейт
  этой задачи.
- НЕ расширять правку на «снимочную триаду» и другие условия файла.
- Новые тесты не добавлять; существующие не ослаблять.

## ПОСЛЕ ПРАВКИ — ВАЖНО

Номера строк в `raft_cm_replication.go` сдвинутся: задачи 004 и 005
сверяются по имени функции и тексту условия «Было» — сообщи в отчёте
величину сдвига (для информации последующим запускам).

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

При противоречии между кодом, задачей, ADR или ревью — НЕ решай сам:
создай ARCHITECTURE_BLOCKER (место, суть), останови этот участок.

## GIT DIFF REVIEW

```bash
    git diff --stat
    git diff
```

Нет случайных изменений, debug-кода, изменений вне scope (дифф задач
001–002 не повреждён). **НЕ коммить** — коммитит владелец; строка
«Без коммита» обязательна в шапке отчёта.

## ФИНАЛЬНЫЙ ОТЧЁТ

`.ai/developer/task-report-S0-003.md` (шапка: задача, статус,
«Без коммита»): что изменено (включая величину сдвига строк);
артефакт «can inline»; команды и результаты (lint, целевой тест -race,
полный прогон, TestLockOwnershipDiscipline); ревью собственного
git diff; блокеры; остаточные риски.

## ФИНАЛЬНЫЙ СТАТУС

Один из: IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED. COMPLETE — только при всех зелёных критериях.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; изменённые файлы; команды и результаты; «can inline»;
подтверждение «Без коммита».
