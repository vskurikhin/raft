# Q9.5 STABILIZATION-0. Разработчик — TASK-S0-012 (документация + финальные гейты)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-012
(веха STABILIZATION-0, решение ADR-S0-003 — Accepted).
Это задача 5 из 5 (финальная); выполняется строго ПОСЛЕ TASK-S0-011.

## Роль
Ты — Senior Go Developer, завершаешь реализацию УТВЕРЖДЁННОГО
архитектурного решения (SA-ревью: раунды 3–5, финал APPROVED).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- Изменяй только то, что предписано задачей (в основном — документация
  и финальные проверки).

## КОНТЕКСТ

ADR-S0-003 реализован задачами 008–011 (лист-пакет contract, инъекция
транспорта, пакеты store/transp, golden-тест меток). Эта задача —
синхронизация документации и финальные гейты всего поднаправления:

- README.md: раздел архитектуры/хранения (устаревшие имена);
- CLAUDE.md: карта файлов корневого пакета (testharness.go не
  существует — фактически harness_test.go; новые файлы aliases.go,
  pkg/raft/{contract,store,transp}), слои, структура тестирования —
  правка CLAUDE.md САНКЦИОНИРОВАНА владельцем (решение от
  2026-09-02, зафиксировано в задаче и ADR);
- конвенция квалификаторов (одна фраза: в производственном коде корня
  — `raft.X`; в store/transp — `contract.X`; в тестах store/transp
  корневые символы — через `raft.`, листовые — через `contract.`).

## SA-ГЕЙТ И ПРЕДУСЛОВИЯ

1. review-round-5.md §5 = APPROVED; ADR-S0-003 Accepted. Иначе —
   ARCHITECTURE_NOT_APPROVED.
2. `.ai/developer/task-report-S0-011.md` завершён
   IMPLEMENTATION_COMPLETE. Иначе — STOP, TASK_ORDER_VIOLATION.
3. Идемпотентность: свой отчёт уже COMPLETE — не повторяй.
4. Незакоммиченный дифф задач 008–011 в дереве: НЕ откатывай.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ

- AGENTS.md, CLAUDE.md (целиком — ты его правишь)
- .ai/architect/decisions.md — ADR-S0-003: Decision п.6, Consequences
- .ai/architect/tasks/TASK-S0-012-docs-sync.md — ЦЕЛИКОМ (точные
  перечни замен по README/CLAUDE.md; критерии полноты доков;
  финальные гейты)
- README.md (разделы по перечню задачи), doc.go

## ПОРЯДОК

1. Правки README.md по перечню задачи (замены «Было→Стало»).
2. Правки CLAUDE.md по перечню задачи: карта файлов (удаление
   testharness.go → harness_test.go; +aliases.go; +строки новых
   пакетов в таблице и в «AI workflow artifacts»/Testing, где
   указано), конвенция квалификаторов — фраза из задачи дословно.
3. doc.go — по задаче (проверь: устаревших имён не содержит;
   правь только если задача предписывает).
4. Финальные гейты поднаправления (по всему диффу 008–012):
   `gofmt -l .` (вне part*/.doc) — пусто; `go vet . ./pkg/...`;
   `golangci-lint run --verbose` — без новых; `go test .` и
   `go test ./pkg/...`; `go test -race .` и `go test -race ./pkg/...`;
   полнота доков — grep-критерии задачи (testharness, старые
   квалификаторы raft.NewFileStorage и т.п. — по списку).
5. git diff; отчёт.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Правки документации МИНИМАЛЬНЫ и точны: только перечни задачи;
  стиль CLAUDE.md сохраняется (английский/русский — как в файле,
  не переводи разделы).
- Конвенция квалификаторов — дословно из задачи (она согласована
  с ADR Decision п.6).
- Никаких изменений производственного кода в этой задаче
  (если гейты вскроют проблему кода — ARCHITECTURE_BLOCKER, не правь).

## КРИТЕРИИ ПРИЁМКИ

- [ ] Все grep-критерии полноты доков задачи — выполнены
      (устаревших имён/файлов в README/CLAUDE.md/doc.go нет);
- [ ] `gofmt -l .` — пусто; `go vet . ./pkg/...` — чисто;
- [ ] `golangci-lint run --verbose` — без новых замечаний;
- [ ] `go test .`, `go test ./pkg/...` — зелёные;
- [ ] `go test -race .`, `go test -race ./pkg/...` — зелёные
      (до ~30+ минут суммарно; таймауты с запасом);
- [ ] **Без коммита**: дифф всего поднаправления (008–012)
      остаётся в рабочем дереве.

## ЗАПРЕТЫ

- НЕ править .ai/ других ролей; НЕ коммитить.
- НЕ расширять правки доков сверх перечней (вкусовые правки — вне).
- Учебные каталоги part* не трогать.

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

Если финальные гейты вскрыли проблему кода/тестов —
ARCHITECTURE_BLOCKER (место, суть), стоп; документационные правки
можно завершить, если они не зависят от блокера.

## GIT DIFF REVIEW + ОТЧЁТ

`git diff --stat` — дифф поднаправления целиком (008–012):
перемещения статусом R, правки — по перечням задач. **НЕ коммить**;
отчёт `.ai/developer/task-report-S0-012.md` (шапка «Без коммита»;
правки доков по перечням; ВСЕ финальные гейты с выводом команд;
блокеры; остаточные риски).

## ФИНАЛЬНЫЙ СТАТУС

IMPLEMENTATION_COMPLETE / BLOCKED / FAILED.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; правки доков (README/CLAUDE.md по пунктам);
все финальные гейты с результатами (вкл. оба -race прогона);
подтверждение «Без коммита» — git status показывает дифф
поднаправления 008–012, готовый к ревью и коммиту владельца.
