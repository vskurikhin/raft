# Q3.2 STABILIZATION-0. Разработчик — TASK-S0-002 (именованные bool в голосовании)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-002
(веха STABILIZATION-0, решение ADR-S0-001 — Accepted).
Это задача 2 из 5; выполняется строго ПОСЛЕ TASK-S0-001; остальные
задачи этого запуска НЕ трогай.

## Роль
Ты — Senior Go Developer, реализуешь УТВЕРЖДЁННОЕ архитектурное
решение, прошедшее независимое ревью Senior Architect (два раунда).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- НЕ расширяй scope задачи без явного основания.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

ADR-S0-001 — рефакторинг громоздких условных выражений с жёстким
контрактом: правка ДОКАЗУЕМО ТОЖДЕСТВЕННА по поведению.

Эта задача — `raft_cm_rpc.go`, два обработчика голосования:

- `RequestVote` (:324, находка BC-008): ввести РОВНО ОДИН именованный
  bool «именование намерения»;
- `RequestPreVote` (:435–436, находка BC-009 частично): именованный
  bool — посимвольный перенос условия отказа.

Голосование — зона Election Safety: ошибка эквивалентности меняет
грант/отказ голоса.

## ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. SA-вердикт: `.ai/senior-architect/review.md`, раздел «Раунд 2»,
   §6 = APPROVED; `.ai/architect/decisions.md` — ADR-S0-001 Accepted.
   Иначе: ARCHITECTURE_NOT_APPROVED, код не трогай.
2. Последовательность: `.ai/developer/task-report-S0-001.md`
   существует и завершён IMPLEMENTATION_COMPLETE. Иначе — STOP
   с сообщением TASK_ORDER_VIOLATION (порядок 001→…→005 обязателен).
3. Идемпотентность: если `task-report-S0-002.md` уже завершён
   COMPLETE — не повторяй, доложи статус.
4. В рабочем дереве лежит незакоммиченный дифф задачи 001
   (transport_tcp.go): НЕ откатывай и не правь его; твой дифф — поверх.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md, CLAUDE.md
- .ai/architect/decisions.md — разделы: Status, «Изменения по ревью
  SA», «Паттерн рефакторинга» (п.3 — именованные bool), «Политика
  сохранения поведения», «Критерии приёмки»
- .ai/architect/tasks/TASK-S0-002-named-bools-vote-handlers.md —
  ЦЕЛИКОМ (особенно Target State с точным кодом, Non-Goals, Risks)
- Код: `raft_cm_rpc.go` — `RequestVote` (:320–348) и
  `RequestPreVote` (:425–440)

## ЗАДАЧА (точный код — в Target State задачи)

1. `RequestVote`, перед :324:

```go
leaderIDKnown := cm.cmState.leaderID >= 0
if leaderIDKnown && cm.cmState.leaderID != args.CandidateID && !args.LeadershipTransfer {
```

   - Имя `leaderIDKnown` ТОЧНОЕ, не уточняется. Имя `leaderKnown`
     НЕ использовать: на :427 уже есть локальный `leaderKnown` —
     ДРУГОЙ предикат («контакт с лидером свежий»,
     `time.Since(leaderLastContact) < electionTimeout()`); омоним
     в соседних обработчиках запрещён критерием приёмки.
   - Второй bool (`candidateIsNotLeader`-вида) НЕ вводится.
   - Вынос первого операнда в именованный bool числа чтений полей
     не меняет ни при каком значении `leaderID`; чтения чистые.
2. `RequestPreVote`, перед :435: `localLogNewer := ` и перенос
   условия посимвольно (знак `>` по индексу сохраняется; точный
   текст — в задаче).
3. Трассировка: `cm.traceLockedLogf` (:325 и :437) остаётся ПЕРВОЙ
   строкой своей ветки, на месте, уровень не меняется.

## АРТЕФАКТ «ДО» (снять ДО правки, приложить к отчёту)

```bash
    grep -n "traceLockedLogf\|traceLogf" raft_cm_rpc.go
```

Вывод «до» и «после»: номера строк могут измениться, состав и
порядок вызовов — нет.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- `logOk` (:337–338, знак `>=`) НЕ тронут; консолидация `logOk` ↔
  `localLogNewer` по де Моргану ЗАПРЕЩЕНА (ловушка границы `>=`/`>`:
  грант/отказ меняются на границе равенства индексов).
- `termOk`/`voteOk` (:335–337) не трогать.
- Порядок операндов в переносимом условии — посимвольно.
- Комментарий перед :324 («Проверка наличия известного лидера…»)
  сохраняется.

## КРИТЕРИИ ПРИЁМКИ

- [ ] В `RequestVote` ровно один новый bool `leaderIDKnown`;
      `leaderKnown` не используется.
- [ ] `localLogNewer` — посимвольный перенос; `logOk` не тронут.
- [ ] Артефакт трасс до/после приложен (состав и порядок совпадают).
- [ ] gofmt/goimports по `raft_cm_rpc.go` — без замечаний.
- [ ] `golangci-lint run --verbose` — без новых замечаний.
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`;
      до ~15 минут, таймауты с запасом; -count — не больше 3).
- [ ] `go test -run '^$' -bench 'BenchmarkCMAppendEntries'
      -benchmem -count=1 .` — allocs/op не хуже 54/87/54
      (Heartbeat/Entries/Conflict). Это контроль СОСЕДНЕГО
      обработчика AppendEntries в том же файле, НЕ гейт
      эквивалентности предмета задачи — задача это знает.
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- НЕ трогать BC-007: условие :121–122 (`PrevLogIndex == -1 || ...`
  с `lookupTermLocked`) — порядко-чувствительный горячий путь,
  отвергнут ADR.
- НЕ консолидировать пару BC-009 (см. выше).
- НЕ переносить/удалять трассировки; НЕ менять их уровень.
- Новые тесты не добавлять; существующие не ослаблять.
- funlen-запас `RequestVote` мал (75/87 строк): при срабатывании
  линтера — STOP → ARCHITECTURE_BLOCKER, никаких «доработок заодно».

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

При противоречии между кодом, задачей, ADR или ревью — НЕ решай сам:
создай ARCHITECTURE_BLOCKER (место, суть), останови этот участок.

## GIT DIFF REVIEW

```bash
    git diff --stat
    git diff
```

Нет случайных изменений, debug-кода, изменений вне scope (в т.ч.
дифф задачи 001 не повреждён). **НЕ коммить** — коммитит владелец;
строка «Без коммита» обязательна в шапке отчёта.

## ФИНАЛЬНЫЙ ОТЧЁТ

`.ai/developer/task-report-S0-002.md` (шапка: задача, статус,
«Без коммита»): что изменено; артефакт трасс до/после; команды
и результаты (lint, бенчмарк, полный прогон); ревью собственного
git diff; блокеры; остаточные риски.

## ФИНАЛЬНЫЙ СТАТУС

Один из: IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED. COMPLETE — только при всех зелёных критериях.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; изменённые файлы; команды и результаты (включая
бенчмарк 54/87/54); артефакт трасс; подтверждение «Без коммита».
