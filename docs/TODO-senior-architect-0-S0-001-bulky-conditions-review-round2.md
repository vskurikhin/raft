# Q2 STABILIZATION-0. Сеньёр Архитектор — раунд 2 ревью ADR-S0-001

## Промпт
Запусти subagent `senior-architect` для независимого review
результата устранения замечаний раунда 1 (Architect отработал
по CHANGES_REQUIRED).

Промпт раунда 1 (завершён, вердикт CHANGES_REQUIRED):
[TODO-senior-architect-0-S0-001-bulky-conditions-review.md](./TODO-senior-architect-0-S0-001-bulky-conditions-review.md)

## Роль: SENIOR ARCHITECT, раунд 2
## Проверка работы Architect по предыдущим замечаниям

Ты — тот же независимый Senior Architect / Principal Engineer,
последний архитектурный барьер перед изменением production code.
Раунд 1 (2026-09-02) вынес CHANGES_REQUIRED: BLOCKER — 0,
HIGH — 2 (SA-001, SA-002), MEDIUM — 6, LOW — 5; идея решения
подтверждена (SA-013: все пять замен тождественны), правился
валидационный слой и тексты задач. Architect внёс правки по
RC-1…RC-13.

Твоя задача в раунде 2 — НЕ пересматривать архитектуру заново,
а верифицировать устранение замечаний и попытаться найти новые
проблемы, внесённые самими правками.

Production code НЕ изменять.
Architecture НЕ исправлять самостоятельно.
TASK НЕ переписывать.
Изменять только `.ai/senior-architect/`.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ

Результаты твоего раунда 1:

    .ai/senior-architect/review.md
    .ai/senior-architect/findings.md
    .ai/senior-architect/risks.md

Обновлённые артефакты Architect (правки раунда 2 отмечены
подразделом «Изменения по ревью SA (раунд 1)» в decisions.md):

    .ai/architect/decisions.md
    .ai/architect/architecture.md
    .ai/architect/tasks/TASK-S0-001-dedup-installsnapshot-check.md
    .ai/architect/tasks/TASK-S0-002-named-bools-vote-handlers.md
    .ai/architect/tasks/TASK-S0-003-predicate-snapshot-hole.md
    .ai/architect/tasks/TASK-S0-004-predicate-inflight-present.md
    .ai/architect/tasks/TASK-S0-005-predicate-leader-for-term.md

Артефакты Analyst, правленные координатором по RC-8/RC-2
(Architect'у файлы Analyst менять запрещено; тексты правок —
из его отчёта раунда 2):

    .ai/analyst/findings.md   (§2, строка BC-006)
    .ai/analyst/risks.md      (§5.2)

Код и grep-проверки — во всех местах, где новые формулировки
заявляют факты (список в таблице ниже).

## РЕШЕНИЯ ARCHITECT ПО РАЗВИЛКАМ (проверь, что проведены последовательно)

- RC-1 — вариант 1: признано отсутствие измеримого гейта на
  изменяемом коде; контроль inlining через `go build -gcflags=-m`
  в отчёте Developer; `BenchmarkCMAppendEntries` оставлен только
  в TASK-S0-002 как контроль соседнего обработчика.
- RC-2 — митигация переписана на фактические барьеры,
  `TestLockOwnershipDiscipline` НЕ расширялся.
- RC-5 — вариант (б): RequestVote не сужали; BC-008
  переклассифицирован в «именование намерения»; один bool
  `leaderIDKnown`.
- RC-9 — Architect утверждает, что в дереве фактические строки
  `:499`/`:529` (у тебя в SA-010 — `:497`/`:528`); номера заменены
  именами функций. Проверь по коду, кто прав, и зафиксируй.

## ГЛАВНЫЙ ВОПРОС РАУНДА 2

    "Устранены ли все замечания раунда 1 — без введения новых?"

Три пункта твоего же §20 раунда 1, которые раунд 2 обязан
подтвердить:

(а) критерии приёмки называют то, что они действительно проверяют;
(б) критерий TASK-S0-005 механически исполним,
    и raft_cm_replication.go:476 выведен из области;
(в) остаток вехи оформлен проверяемым списком.

## ОСОБОЕ ПРАВИЛО: ПОДТВЕРЖДЕНИЕ ВЛАДЕЛЬЦА

Подтверждение владельцем раздела «Остаток вехи» — внешнее условие,
не часть архитектуры. Если оно ещё не зафиксировано — НЕ считай это
дефектом Architect и не понижай за него вердикт: отметь отдельной
строкой в вердикте как открытое условие передачи задач Developer.

## ПРОВЕРКА КАЖДОГО RC

Для каждого — статусы RESOLVED / PARTIALLY RESOLVED /
NOT RESOLVED / SUPERSEDED + evidence. Проверяй по коду и grep,
не по заявлениям документов.

| RC       | Что проверить                                                                                                                                                                                                                                                                                                                           | Как                                                                                |
|----------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------|
| RC-1     | «бенчмарк-гейт обязателен» и 54/87/54 как гейт эквивалентности убраны из TASK-S0-003/004/005; BenchmarkCMAppendEntries остался только в TASK-S0-002 с оговоркой о непокрытии; шаг `go build -gcflags=-m` (can inline) есть; формулировки согласованы в decisions.md (Критерии приёмки, Risks, Why) и architecture.md (Testing Strategy) | grep по задачам и ADR; чтение разделов                                             |
| RC-2     | Утверждение «проверяется TestLockOwnershipDiscipline» убрано из decisions.md (Паттерн п.1; Risks), architecture.md (Concurrency), TASK-S0-003/004/005; записаны три фактических барьера; про тест — только симметрия Lock/Unlock; `.ai/analyst/risks.md` §5.2 переписан                                                                 | grep `TestLockOwnershipDiscipline` по `.ai/`; сличение с `lock_discipline_test.go` |
| RC-3     | Критерий №1 TASK-S0-005 механический: после правки `grep -n "cmState.currentTerm == \|cmState.currentTerm != " raft_cm_replication.go` — ровно одно совпадение (тело предиката); до правки шаблон даёт 8 совпадений + обратная форма `:333`; `:476` и `:379` в Non-Goals с причинами; вне файла критерий не нужен                       | выполни оба grep сам; чтение Non-Goals                                             |
| RC-4     | В TASK-S0-002 и TASK-S0-005 есть шаг с артефактом: перечень `grep -n "traceLockedLogf\|traceLogf" <файл>` до/после в отчёте Developer; опорная точка в TASK-S0-005 (5 traceLockedLogf + 8 traceLogf) верна                                                                                                                              | grep кода; чтение задач                                                            |
| RC-5     | Ровно один bool `leaderIDKnown` (точное имя, без `candidateIsNotLeader`, без «допускается уточнить»); омоним `leaderKnown` :427 разоблачён; ADR (Паттерн п.3, Current State, приложение строка 5) и architecture.md согласованы; замена остаётся тождественной                                                                          | grep задач и ADR; код :320–348                                                     |
| RC-6     | Поимённые тесты в Tests задач 003/004/005 (11 шт.) и требование целевого `go test -run '<список>' -race .` в отчёте                                                                                                                                                                                                                     | `go test . -list` — существование                                                  |
| RC-7     | Раздел «Остаток вехи» в decisions.md: списком, с итоговым текстом остающихся условий `:332`/`:659`, пометкой о подтверждении владельцем; architecture.md (Risks) ссылается на раздел, «доведён до владельца» убрано                                                                                                                     | чтение обоих файлов                                                                |
| RC-8     | findings.md §2 строка BC-006: канонический перечень 9 вхождений (включая 332–333 и 659–660), raft_cm_leader.go:709 отнесён к BC-015                                                                                                                                                                                                     | сличение с §4.1                                                                    |
| RC-9     | Номера заменены именами функций (`leaderSendAEs`, `leaderSendAEsToPeerIfIdle`); фактические строки в дереве                                                                                                                                                                                                                             | `grep -n "inflightAE\[peerID\] == nil" raft_cm_replication.go`                     |
| RC-10    | TASK-S0-001 Concurrency: фиксация передачи `cmd` в consumerCh между проверками и «потребитель только читает DataSize» (с адресами)                                                                                                                                                                                                      | grep `DataSize` по коду                                                            |
| RC-11–13 | Doc-оговорки трёх предикатов: prevInSnapshotHoleLocked — фактические операнды; isLeaderForTermLocked — не заменяет сверку reply.Term; inflightPresentLocked — существование флага не даёт права сбрасывать                                                                                                                              | чтение Target State задач                                                          |

## НОВЫЕ РИСКИ, ВНЕСЁННЫЕ ПРАВКАМИ (обязательный adversarial-минимум)

1. Grep-критерий RC-3: не даёт ли шаблон лишних совпадений в других
   формах (`savedCurrentTerm`-варианты, тесты)? Выполни grep по
   текущему дереву и сверь с заявлением Architect.
2. RC-5: не протащил ли пересмотр BC-008 второй bool обратно;
   не появилось ли противоречие между «именование намерения»
   в ADR и старой классификацией в реестре Analyst
   (`.ai/analyst/findings.md` §2 категория (a) у BC-008 —
   допустимое расхождение или требует оговорки?).
3. RC-1: не осталось ли в задачах 003–005 прочих ссылок на
   бенчмарки как критерий (grep `54/87/54`, `bench`).
4. Не задеты ли неизменяемые части (§20 раунда 1): состав пяти
   задач, имена предикатов, таблица замен TASK-S0-005 (9 строк
   «Было/Стало»), разделение уровней TASK-S0-003, границы
   критической секции defer TASK-S0-004, таблицы «Отвергнуто»/
   «Оставить как есть», порядок 001→…→005.
5. `git status`: производственный код и тесты не изменены
   (изменённые отслеживаемые файлы отсутствуют).

## ФОРМАТ .ai/senior-architect/review.md

Обнови существующий файл: добавь наверх раздел «Раунд 2»
(дата, что проверено) со структурой:

    ### Раунд 2 (2026-09-XX)

    #### 1. Executive Summary
    #### 2. Верификация RC-1…RC-13 (таблица: RC → статус → evidence)
    #### 3. Статусы находок раунда 1 (SA-001…SA-013)
    #### 4. Новые находки раунда 2 (SA-2XX, формат карточки раунда 1;
             если нет — явно «новых находок нет»)
    #### 5. Подтверждение пунктов (а)/(б)/(в) §20
    #### 6. Final Verdict

Содержимое раунда 1 не удаляй и не переписывай — раунд 2
дописывается сверху. Не изменяй другие файлы, кроме
`.ai/senior-architect/`.

## FINAL VERDICT

Обязательно один из:

    APPROVED
    CHANGES_REQUIRED
    REJECTED

### APPROVED — только если

- все обязательные RC-1…RC-7 (и проверенные RC-8…RC-13) RESOLVED;
- новые находки отсутствуют или LOW без влияния на передачу задач;
- пункт (а) подтверждён: критерии называют то, что проверяют;
- пункт (б) подтверждён: grep-критерий механически исполним,
  `:476` выведен из области;
- неизменяемые части не тронуты.

Пункт (в): подтверждение владельцем остатка вехи — внешнее условие
(см. выше); при его отсутствии отметь его отдельной строкой
«открытое условие передачи задач Developer».

### CHANGES_REQUIRED — если

хотя бы один обязательный RC не закрыт, или правки внесли новую
проблему (MEDIUM и выше), или неизменяемая часть задета.

## ПОСЛЕ ВЕРДИКТА (если APPROVED)

Укажи явно:

- ADR-S0-001 в `.ai/architect/decisions.md` может быть переведён
  Architect'ом из Proposed в Accepted;
- TASK-S0-001…005 передаются Developer'у строго последовательно
  001 → 002 → 003 → 004 → 005 (003/004/005 — один файл,
  параллельное выполнение запрещено), с полным набором критериев
  приёмки после каждой задачи и без коммита (дифф в рабочем дереве,
  коммитит владелец).

## ПРАВИЛА РАУНДА 2

- Краткость: раунд 2 существенно короче раунда 1 (твоё же §20).
  Не пересматривай заново то, что уже верифицировано в раунде 1
  (SA-013), если правки этого не касались.
- Каждое утверждение о коде проверяй сам (grep/чтение/сборка);
  заявления документов не являются evidence.
- Каждое замечание: конкретное, доказуемое, с failure scenario
  и required action.
- Лучше CHANGES_REQUIRED, чем преждевременный APPROVED — но не
  выдавай замечания ради количества.
