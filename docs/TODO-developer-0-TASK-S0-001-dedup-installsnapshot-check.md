# Q3.1 STABILIZATION-0. Разработчик — TASK-S0-001 (дубль InstallSnapshot в транспорте)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-001
(веха STABILIZATION-0, решение ADR-S0-001 — Accepted).
Это задача 1 из 5; остальные задачи этого запуска НЕ трогай.

## Роль
Ты — Senior Go Developer, реализуешь УТВЕРЖДЁННОЕ архитектурное
решение, прошедшее независимое ревью Senior Architect (два раунда).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- НЕ расширяй scope задачи без явного основания.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

ADR-S0-001 — рефакторинг громоздких условных выражений с жёстким
контрактом: правка ДОКАЗУЕМО ТОЖДЕСТВЕННА по поведению.

Эта задача — минимальный риск, без cm.mu: в `handleCommand`
(`transport_tcp.go`) пара «type-assertion к `*InstallSnapshotRequest`
&& `DataSize > 0`» вычисляется дважды (:549 — построение snapReader,
:579 — масштабирование respTimeout); между проверками `cmd` не
переприсваивается. Заменить одним assertion + одним именованным bool.

## ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. SA-вердикт: `.ai/senior-architect/review.md`, раздел «Раунд 2»,
   §6 = APPROVED; `.ai/architect/decisions.md` — ADR-S0-001 Accepted.
   Иначе: остановись, сообщи ARCHITECTURE_NOT_APPROVED, код не трогай.
2. Идемпотентность: если `.ai/developer/task-report-S0-001.md` уже
   существует и завершён IMPLEMENTATION_COMPLETE — не повторяй работу,
   доложи статус.
3. В рабочем дереве может лежать незакоммиченный дифф предыдущих
   сессий (коммитит владелец): НЕ откатывай и не правь чужое; твой
   дифф — поверх.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md (стиль, терминология комментариев)
- CLAUDE.md
- .ai/architect/decisions.md — разделы: Status, «Изменения по ревью
  SA», «Паттерн рефакторинга», «Политика сохранения поведения»,
  «Критерии приёмки»
- .ai/architect/tasks/TASK-S0-001-dedup-installsnapshot-check.md —
  ЦЕЛИКОМ (Goal, Target State, Implementation, Non-Goals, Risks,
  Acceptance Criteria)
- Код: `transport_tcp.go`, функция `handleCommand` (:520–607)

## ЗАДАЧА

Перед объявлением `snapReader` (:548) ввести:

```go
snapReq, isSnapReq := cmd.(*InstallSnapshotRequest)
hasSnapshotData := isSnapReq && snapReq.DataSize > 0
```

Оба места (:549 и :579) используют `hasSnapshotData`; внутри веток —
`snapReq.DataSize`. Допустима вариация имени bool по смыслу (стиль —
как `snapshotNeeded` в `raft_cm_replication.go:57`); критерий
приёмки от имени не зависит.

## ПОРЯДОК

1. Изучи `handleCommand`; сверься с утверждением задачи, что `cmd`
   не переприсваивается между :549 и :579 (присваивания — только
   в switch :528–539; heartbeat fast-path :555–566 только читает).
2. Введи пару «assertion + bool» до первого использования и ДО
   `goto RESP` (:563) — ограничение Go на goto и область видимости.
3. Замени оба условия на `if hasSnapshotData { ... }`.
4. Комментарий :542–547 (разделение источников данных) — без изменений.
5. gofmt/goimports по файлу.
6. Выполни критерии приёмки (ниже).
7. `git diff` — ревью собственных изменений.
8. Заполни `.ai/developer/task-report-S0-001.md`.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Ровно ОДНО type-assertion к `*InstallSnapshotRequest`;
  `DataSize > 0` вычислено один раз.
- `var snapReader io.ReadCloser` и nil-логика snapReader не изменились.
- `goto RESP` и heartbeat fast-path (:555–566, эвристика BC-016) —
  не тронуты.
- Раздел Concurrency задачи (happens-before): `cmd` передаётся в
  `t.consumerCh` (:570) между двумя проверками; кэширование признака
  допустимо, потому что потребитель (`raft_cm_snapshot.go` :123, :262)
  поле `DataSize` ТОЛЬКО читает (единственная запись — лидерская,
  `raft_cm_replication.go:555`). НЕ добавляй запись в `DataSize`
  на стороне потребителя.

## КРИТЕРИИ ПРИЁМКИ

- [ ] Одно assertion; условие вычислено один раз (см. выше).
- [ ] gofmt/goimports по `transport_tcp.go` — без замечаний.
- [ ] `golangci-lint run --verbose` — без новых замечаний.
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`:
      учебные каталоги part1…part5kv не прогонять; полный прогон
      до ~15 минут — таймауты команд ставь с запасом).
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- Любой другой рефакторинг `handleCommand`, включая BC-016 (:557) —
  отвергнут ADR-S0-001.
- Вынос условия в метод/хелпер.
- Новые тесты не добавлять; существующие не ослаблять.

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

При противоречии между кодом, задачей, ADR или ревью — НЕ решай сам:
создай ARCHITECTURE_BLOCKER (место, суть), останови этот участок.

## GIT DIFF REVIEW

```bash
    git diff --stat
    git diff
```

Нет случайных изменений, временных файлов, debug-кода, изменений
вне scope. **НЕ коммить** (ни текущую ветку, ни main) — коммитит
владелец; строка «Без коммита» обязательна в шапке отчёта.

## ФИНАЛЬНЫЙ ОТЧЁТ

`.ai/developer/task-report-S0-001.md` (шапка: задача, статус,
«Без коммита»): что изменено; команды и результаты (lint, полный
прогон); ревью собственного git diff; блокеры; остаточные риски.

## ФИНАЛЬНЫЙ СТАТУС

Один из: IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED. COMPLETE — только при всех зелёных критериях.

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; изменённые файлы; команды и результаты; подтверждение
«Без коммита» (git status — только ожидаемый дифф).
