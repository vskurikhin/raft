# Q5.2 STABILIZATION-0. Разработчик — TASK-S0-007 (суффикс Locked, пакетные функции)

## Промпт
Запусти subagent `developer` для реализации ОДНОЙ задачи TASK-S0-007
(веха STABILIZATION-0, решение ADR-S0-002 — Proposed).
Это задача 2 из 2 поднаправления «Locked-имена»; выполняется строго
ПОСЛЕ TASK-S0-006.

## РАЗРЕШЕНИЕ НА ДОСРОЧНЫЙ СТАРТ (решение владельца 2026-09-02)

SA-ревью ADR-S0-002 ещё НЕ проведено. Владелец явным решением
разрешил реализацию ДО вердикта: замечания, если появятся,
устраняются после — правкой архитектуры и кода. Следствия для тебя:

- это НЕ отменяет ARCHITECTURE_BLOCKER-дисциплину;
- дифф может быть переработан по итогам ревью: все артефакты
  (grep-выводы) сохраняй в отчёте;
- ADR-S0-002 остаётся в статусе Proposed — статусы задач от этого
  не зависят.

## Роль
Ты — Senior Go Developer, реализуешь архитектурное решение
(переименования по контракту дисциплины блокировок).

- НЕ выполняй собственное архитектурное проектирование вместо Architect.
- НЕ меняй архитектурные решения самостоятельно.
- Производственный код изменяй ТОЛЬКО в объёме, предписанном задачей.

## КОНТЕКСТ

Класс-решение ADR-S0-002: суффикс `Locked` применим и к ПАКЕТНЫМ
функциям (данные cm приходят параметрами); doc обязан называть
владельца данных («мьютекс владельца карты/очереди»).

Эта задача — ТРИ переименования (только идентификаторы и doc):

    incPeerCount               → incPeerCountLocked
    peerSum                    → peerSumLocked
    hasPendingVerifyAfterEpoch → hasPendingVerifyAfterEpochLocked

Полные таблицы «Было → Стало» (включая многострочные вызовы
`incPeerCount` в `raft_cm_snapshot.go`) — в задаче; таблица —
исчерпывающий контракт. Места вызовов лежат в зонах, правившихся
задачами S0-003/004/005, — якоря по имени функции, не по номерам
строк.

## ПРЕДУСЛОВИЯ (проверь ДО правки кода)

1. Последовательность: `.ai/developer/task-report-S0-006.md`
   завершён IMPLEMENTATION_COMPLETE (и S0-005 ранее). Иначе —
   STOP с сообщением TASK_ORDER_VIOLATION.
2. `.ai/architect/decisions.md` содержит ADR-S0-002 (Proposed
   допустим — досрочный старт, см. выше). Нет раздела —
   ARCHITECTURE_NOT_APPROVED.
3. Идемпотентность: `task-report-S0-007.md` уже COMPLETE — не
   повторяй, доложи статус.
4. Незакоммиченный дифф предыдущих задач в дереве: НЕ откатывай
   и не правь чужое; твой дифф — поверх.
5. Места вызовов ищи по имени функции и тексту «Было»; номера
   строк задачи ориентировочные.

## ОБЯЗАТЕЛЬНО ПРОЧИТАТЬ ПЕРЕД ИЗМЕНЕНИЕМ КОДА

- AGENTS.md — «Дисциплина блокировок» + терминология
- CLAUDE.md
- .ai/architect/decisions.md — ADR-S0-002: Decision п.2 (класс
  пакетных функций), п.3–4; Alternatives; Risks
- .ai/architect/tasks/TASK-S0-007-locked-suffix-package-functions.md —
  ЦЕЛИКОМ (таблицы замен, дословные doc-тексты с формулировкой
  владельца данных, Non-Goals, Risks, Acceptance Criteria)
- Код: `raft_cm_metrics.go` (обе функции), `raft_cm_replication.go`
  (`hasPendingVerifyAfterEpoch` и зоны вызовов `incPeerCount`),
  `raft_cm_snapshot.go` (многострочный вызов)

## ПОРЯДОК

1. Сними базовые артефакты «до» (в отчёт):

```bash
    grep -rn -w 'incPeerCount' --include='*.go' . | grep -v '/part'
    grep -rn -w 'peerSum' --include='*.go' . | grep -v '/part'
    grep -rn -w 'hasPendingVerifyAfterEpoch' --include='*.go' . | grep -v '/part'
```

   Сверь с таблицей задачи: вхождение вне таблицы = STOP →
   ARCHITECTURE_BLOCKER.
2. Переименуй по таблицам: определения, все вызовы (включая
   многострочные), тестовые и текстовые вхождения из таблицы.
3. Приведи doc к каноническим текстам ДОСЛОВНО из задачи —
   с формулировкой владельца данных («Требует удержания cm.mu —
   мьютекса владельца карты/очереди» — точный текст в задаче).
4. gofmt/goimports по затронутым файлам.
5. Выполни критерии приёмки.
6. `git diff` — ревью собственных изменений.
7. Заполни `.ai/developer/task-report-S0-007.md`.

## КЛЮЧЕВЫЕ КОНТРОЛИ

- Замена меняет ТОЛЬКО идентификаторы и doc: тела, порядок
  операндов, условия — неприкосновенны.
- `hasPendingVerifyAfterEpoch` формализуется впервые (контракта
  не было): doc — только текст из задачи, без отсебятины.
- Зоны вызовов в `raft_cm_replication.go` недавно правились
  задачами S0: сверься с фактическим кодом, а не с памятью о нём.
- Учебные каталоги part* не трогать.

## КРИТЕРИИ ПРИЁМКИ

- [ ] Для КАЖДОГО из трёх старых имён:

      grep -rn -w '<старое имя>' --include='*.go' . | grep -v '/part'

      — пусто (вывод приложить).
- [ ] Канонические doc с владельцем данных у всех трёх (сверка
      с задачей дословно).
- [ ] gofmt/goimports по затронутым файлам — без замечаний.
- [ ] `golangci-lint run --verbose` — без новых замечаний.
- [ ] `go test .` и `go test ./pkg/...` — зелёные (НЕ `./...`;
      до ~15 минут, таймауты с запасом; -count ≤ 3).
- [ ] `go test -run TestLockOwnershipDiscipline .` — зелёный
      (формулировка честная: симметрия Lock/Unlock, не суффиксы).
- [ ] Целевые прогоны тестов из раздела Tests задачи с `-race`
      (зоны verify/репликации затронуты).
- [ ] **Без коммита**: дифф остаётся в рабочем дереве.

## ЗАПРЕТЫ

- НЕ переименовывать ничего сверх трёх имён.
- НЕ изменять тела, порядок вызовов, условия, трассировки.
- НЕ добавлять тесты; НЕ ослаблять существующие.
- НЕ «заодно» канонизировать другие комментарии в тронутых файлах.

## ОБРАБОТКА АРХИТЕКТУРНЫХ ПРОБЛЕМ

При противоречии между кодом, задачей, ADR — НЕ решай сам:
ARCHITECTURE_BLOCKER (место, суть), останови участок. Особенно:
вхождение имени вне таблицы; расхождение фактического кода зоны
с текстом «Было» (дерево могло уйти вперёд относительно задачи).

## GIT DIFF REVIEW

```bash
    git diff --stat
    git diff
```

Дифф содержит только переименования и doc. **НЕ коммить** —
коммитит владелец; строка «Без коммита» обязательна в шапке
отчёта.

## ФИНАЛЬНЫЙ ОТЧЁТ

`.ai/developer/task-report-S0-007.md` (шапка: задача, статус,
«Без коммита», пометка досрочного старта): базовые grep «до»
и контрольные «после» по трём именам; соответствие перечня
таблице; команды и результаты; ревью собственного git diff;
блокеры; остаточные риски.

## ФИНАЛЬНЫЙ СТАТУС

Один из: IMPLEMENTATION_COMPLETE / IMPLEMENTATION_BLOCKED /
IMPLEMENTATION_FAILED. COMPLETE — только при всех зелёных
критериях.

## ЭТО ФИНАЛЬНАЯ ЗАДАЧА ПОДНАПРАВЛЕНИЯ

После её завершения дополнительно по всему накопленному диффу
(006 + 007):

```bash
    go vet .
    go test -race .
    git diff --stat
```

## ФИНАЛЬНОЕ СООБЩЕНИЕ МНЕ

Кратко: статус; три grep-критерия пусты; изменённые файлы;
команды и результаты (включая финальные go vet / go test -race);
блокеры; подтверждение «Без коммита».
